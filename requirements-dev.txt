# Yalnızca GELİŞTİRME/DOĞRULAMA araçları. `requirements.txt`'ten AYRI tutulur.
#
# Gerekçe (AGENTS.md §1.2): requirements.txt ürünün ÇALIŞMA ANI bağımlılık
# listesidir ve offline garantisinin kapsamındadır. Buradakiler ürün yolunda
# hiç import edilmez; yalnızca testler ve `eval/ui_proof.py` çalıştırılırken
# yüklenir. `eval/offline_proof.py`'nin ölçtüğü koşumda bu paketlerin hiçbiri
# devrede değildir -- 0 soket iddiası bu ayrım sayesinde bozulmaz.
#
#   .venv/bin/pip install -r requirements-dev.txt
#   .venv/bin/playwright install chromium     # tarayıcı ikilisi (~130 MB)

# backend/tests koşucusu. Buraya .github/workflows/gates.yml kurabilsin diye
# yazıldı; daha önce yalnızca yerel .venv'de kuruluydu ve hiçbir dosyada
# anılmıyordu.
pytest>=9.1

playwright>=1.40

# Gömülü fontların Türkçe kapsamını denetleyen regresyon testi için
# (backend/tests/test_font_coverage.py). Faz 1 Archivo'ya latin+latin-ext
# birleştirmesini yapmış ama JetBrains Mono'ya yapmamıştı; sonuç, mono
# metinlerde "ş/ğ/İ" harflerinin sistem fontuna düşmesiydi (ölçüldü).
# Ürün yolunda HİÇ import edilmez -- font birleştirme dev-zamanı bir iş.
#
# `[woff]` EKSTRASI ZORUNLU, süs değil: woff2 çözücüsü `brotli` uzantısını
# istiyor ve çıplak `fonttools` onu getirmiyor. Yerelde başka bir paket
# brotli'yi kurduğu için testler geçiyordu; CI'ın TEMİZ kurulumunda
# `ImportError: No module named brotli` ile düştü. Tam olarak CI'ın var
# olma sebebi -- ve "yerelde çalışıyordu"nun neden bir kanıt olmadığının
# ölçülmüş örneği.
fonttools[woff]>=4.60
